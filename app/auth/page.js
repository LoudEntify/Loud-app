import Auth from '../../components/Auth';

export const metadata = {
  title: 'Log in · Loudentify pilot',
  description: 'Mock sign in / sign up',
};

export default function AuthPage() {
  return (
    <main>
      <Auth />
    </main>
  );
}
